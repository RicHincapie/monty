#include "monty.h"
/**
 * exec_hash - function to ignore comments
 * @top: pointer to the top of the stack
 * @line_number: line number of the open file
 * Return: void
 */
void exec_hash(__attribute__((unused)) stack_t **top, unsigned int line_number)
{
	line_number += 0
	;
}
